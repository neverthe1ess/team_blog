# 백엔드 연동 기초: CORS & Fetch

## 1. CORS(교차 출처 리소스 공유)란?

**"브라우저의 보안 경비원"**입니다. 👮‍♂️
웹 브라우저는 기본적으로 **다른 출처(Port가 다르면 다른 곳!)**에서 데이터를 가져오는 것을 막습니다.

- Frontend: `localhost:3001`
- Backend: `localhost:3000`
  ➡ **"어? 너네 집 주소가 다르네? 수상하니까 차단!"** (CORS 에러)

### 해결책

백엔드(`NestJS`)에서 **"3001번은 내 친구니까 허락해줘!"**라고 설정해야 합니다.

## 2. 데이터 가져오기 (Fetch)

브라우저 내장 함수인 `fetch`를 사용합니다.

```tsx
// 1. 데이터 요청
const response = await fetch("http://localhost:3000/posts");

// 2. JSON으로 변환
const data = await response.json();
```

## 3. React에서 사용하기 (useEffect)

데이터는 **"화면이 켜지자마자"** 가져와야겠죠?
그래서 **`useEffect`** 안에서 `fetch`를 실행합니다.

```tsx
useEffect(() => {
  fetch("http://localhost:3000/posts")
    .then((res) => res.json())
    .then((data) => setPosts(data));
}, []);
```
