# 🎓 10교시: 동적 라우팅 (Dynamic Routing) 🔀

"글이 100개면 `page.tsx` 파일도 100개 만들어야 하나요?"
아닙니다! **틀(Template)** 하나만 만들어두고 내용만 갈아끼우는 방식을 사용합니다.

## 1. 꺽쇠 괄호 `[ ]` 의 마법 ✨

Next.js에서 폴더 이름에 대괄호 `[]`를 쓰면 **"여긴 아무거나 다 들어올 수 있어!"** 라는 뜻입니다.

- **폴더 구조**: `app/posts/[id]/page.tsx`
- **가능한 주소**:
  - `/posts/1` (id = "1")
  - `/posts/99` (id = "99")
  - `/posts/banana` (id = "banana")

## 2. 주소창 숫자 꺼내기 (`params`) 🎣

누가 `/posts/3`으로 들어왔을 때, 숫자 **3**을 꺼내는 방법입니다.

```typescript
// app/posts/[id]/page.tsx

// 1. props로 { params }를 받습니다.
export default function DetailedPage({ params } : { params: { id: string } }) {

  // 2. params.id에 숫자가 들어있습니다.
  return <h1> 글 번호: {params.id} </h1>;
}
```

## 3. 데이터를 가져오는 순서 🔄

1.  사용자가 `/posts/5` 접속
2.  Next.js가 `params.id` ("5")를 꺼냄
3.  `useEffect`에서 백엔드에 `GET /posts/5` 요청
4.  백엔드가 5번 글 내용(`json`)을 줌
5.  화면에 보여줌!

---

**오늘의 미션**:

1.  `frontend/src/app/posts/[id]/page.tsx` 만들기
2.  `params.id`로 백엔드에서 글 가져오기
3.  메인 페이지(`page.tsx`)의 글 제목을 클릭하면 이리 이동하게 하기 (`router.push('/posts/' + post.id)`)
